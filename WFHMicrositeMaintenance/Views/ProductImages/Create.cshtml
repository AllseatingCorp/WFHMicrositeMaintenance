@model WFHMicrositeMaintenance.Models.ProductImage

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Product Image</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form enctype="multipart/form-data" asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ProductId" />
            @if (Model.Options1.Count() != 0)
            {
                <div class="form-group">
                    <label asp-for="ProductOption1Id" class="control-label"></label>
                    <select asp-for="ProductOption1Id" asp-items="Model.Options1" class="form-control"></select>
                    <span asp-validation-for="ProductOption1Id" class="text-danger"></span>
                </div>
            }
            else
            {
                <input type="hidden" asp-for="ProductOption1Id" />
            }
            @if (Model.Options2.Count() != 0)
            {
                <div class="form-group">
                    <label asp-for="ProductOption2Id" class="control-label"></label>
                    <select asp-for="ProductOption2Id" asp-items="Model.Options2" class="form-control"></select>
                    <span asp-validation-for="ProductOption2Id" class="text-danger"></span>
                </div>
            }
            else
            {
                <input type="hidden" asp-for="ProductOption2Id" />
            }
            @if (Model.Options3.Count() != 0)
            {
                <div class="form-group">
                    <label asp-for="ProductOption3Id" class="control-label"></label>
                    <select asp-for="ProductOption3Id" asp-items="Model.Options3" class="form-control"></select>
                    <span asp-validation-for="ProductOption3Id" class="text-danger"></span>
                </div>
            }
            else
            {
                <input type="hidden" asp-for="ProductOption3Id" />
            }
            <div class="form-group">
                <label asp-for="FormFile" class="control-label"></label>
                <input asp-for="FormFile" class="form-control" />
                <span asp-validation-for="FormFile" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" asp-route-id="@Model.ProductId">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
